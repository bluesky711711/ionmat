export interface UnsplashImage {
    thumbUrl: string;
    author: string;
    authorUrl: string;
    fullSizeUrl: string;
    width: number;
    height: number;
}

export interface FileImage {
    imageUrl: string;
    width: number;
    height: number;
}